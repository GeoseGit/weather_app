name: weather_app
description: "Pet-project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  http: ^1.5.0
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: "^2.3.3"

flutter:
  uses-material-design: true
  assets:
  - lib/assets/

flutter_icons:
  android: true
  ios: true
  image_path: "lib/assets/icon_app.png"

flutter_native_splash:
  color: "#bbbbbb"  
  image: "alib/assets/icon_app.png"
  android: true